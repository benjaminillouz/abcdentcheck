# Variables d'environnement pour ABCDent Checker
# Copiez ce fichier en .env et remplissez les valeurs

# Credentials ABCDent (obligatoires)
ABCDENT_USERNAME=haggiag@cmdnop.fr
ABCDENT_PASSWORD=@Cemedis1234

# Mode de développement (optionnel)
NODE_ENV=development

# Capture d'écran pour debug (optionnel)
CAPTURE_SCREENSHOT=false

# Port pour le développement local (optionnel)
PORT=8080
